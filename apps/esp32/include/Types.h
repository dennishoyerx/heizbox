
/*
struct HeaterState {
    bool isHeating;
    float temperature;
    int power;
};

enum class EventType {
    SENSOR_TRIGGERED,
    HEATER_STARTED,
    HEATER_STOPPED,
    SCREEN_CHANGED,
    WIFI_CONNECTED,
    WIFI_DISCONNECTED,
    STATS_UPDATED
};

template<typename T>
struct Option {
    T initial,
    T max,
    T min,
    T step
};

struct displaycf {
    Option<int> brightness{.initial = 100};
};*/