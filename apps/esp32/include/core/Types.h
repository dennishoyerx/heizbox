// TODO: Add shared type definitions here

struct HeaterState {
    bool isHeating;
    float temperature;
    uint8_t power;
};

/*enum class EventType {
    SENSOR_TRIGGERED,
    HEATER_STARTED,
    HEATER_STOPPED,
    SCREEN_CHANGED,
    WIFI_CONNECTED,
    WIFI_DISCONNECTED,
    STATS_UPDATED
};*/