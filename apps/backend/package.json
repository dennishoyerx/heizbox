{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "@sentry/cloudflare": "^10.19.0",
    "hono": "^4.9.10",
    "wrangler": "^4.42.1"
  },
  "devDependencies": {
    "better-sqlite3": "^12.4.1",
    "sqlite": "^5.1.1"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --minify --dry-run --outdir=dist",
    "deploy": "wrangler deploy --minify",
    "test": "vitest run",
    "monitor": "wrangler tail --format json",
    "monitor:script": "wrangler tail | node formatLogs.js",
    "sync": "node scripts/db-sync.mjs"
  }
}
